import 'package:flutter/material.dart';

class Y extends StatelessWidget {
  const Y({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Y"),
      ),
    );
  }
}
